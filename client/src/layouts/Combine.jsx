import { Outlet } from "react-router-dom";

export default function Combine({}) {
    return (
        <>
            <h2>
                Combine
            </h2>
            <Outlet />
        </>
    )
}