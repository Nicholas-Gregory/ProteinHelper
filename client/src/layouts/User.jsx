import { Outlet } from "react-router-dom";

export default function User({}) {
    return (
        <>
            <h1>
                User
            </h1>
            
            <Outlet />
        </>
    )
}